type PoolEntity @entity {
  id: ID!
  poolId: Bytes! # address
  capacity: BigInt!
  endTime: BigInt!
  ticketValue: BigInt!
}
type StakeEntity @entity {
  id: ID!
  pool: Bytes! # address
  staker: Bytes!
}
